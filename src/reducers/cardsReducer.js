import {CARDS} from '../constants';

const cardsReducer = (state = [], action) => {
  // switch(action.type) {
  //   case CARDS.LOAD: {
  //     debugger;
  //     return state;
  //   }
  //   default: {
  //     debugger;
  //     return state;
  //   }
  // }
  debugger;
  return state;
};

export default cardsReducer;
